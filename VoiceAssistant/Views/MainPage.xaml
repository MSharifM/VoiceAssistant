<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="VoiceAssistant.MainPage">

    <ScrollView>
        <Grid Padding="30,0" RowSpacing="25" ColumnSpacing="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Row 0: Voice Animation -->
            <Image Grid.Row="0" Grid.ColumnSpan="2"
                   Source="voice_animation.gif"
                   IsAnimationPlaying="True"
                   HeightRequest="185"
                   Aspect="AspectFit"
                   SemanticProperties.Description="dot net bot in a hovercraft number nine" />

            <!-- Row 1: Hello World -->
            <Label Grid.Row="1" Grid.ColumnSpan="2"
                   Text="Hello, World!"
                   Style="{StaticResource Headline}"
                   SemanticProperties.HeadingLevel="Level1" />

            <!-- Row 2: Welcome -->
            <Label Grid.Row="2" Grid.ColumnSpan="2"
                   Text="Welcome to &#10;.NET Multi-platform App UI"
                   Style="{StaticResource SubHeadline}"
                   SemanticProperties.HeadingLevel="Level2"
                   SemanticProperties.Description="Welcome to dot net Multi platform App U I" />

            <!-- Row 3: Buttons -->
            <HorizontalStackLayout Grid.Row="3" Grid.ColumnSpan="2" 
                                   Spacing="20" HorizontalOptions="Fill">
                <Button x:Name="btnStart"
                        Text="Start" 
                        SemanticProperties.Hint="Counts the number of times you click"
                        Clicked="OnCounterClicked"
                        HorizontalOptions="FillAndExpand" />

                <Button x:Name="btnStop"
                        Text="Stop" 
                        SemanticProperties.Hint="Counts the number of times you click"
                        Clicked="OnCounterClicked"
                        HorizontalOptions="FillAndExpand" />
            </HorizontalStackLayout>

            <!-- Row 4: Log and Talking Animation -->
            <Label x:Name="lblLog"
                   Grid.Row="4" Grid.Column="0"
                   Text="log"
                   VerticalOptions="Center"/>

            <Image Grid.Row="4" Grid.Column="1"
                   Source="is_talking.gif"
                   IsAnimationPlaying="True"
                   Aspect="AspectFit"
                   HeightRequest="50"
                   VerticalOptions="Center"/>

        </Grid>
    </ScrollView>

</ContentPage>